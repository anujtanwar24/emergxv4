import { createChatBotMessage } from "react-chatbot-kit";



const config = {
 
    botName: "EmergX-bot",
  initialMessages: [createChatBotMessage('Offline search your emergency') ]
}

export default config